"""Headless research experiments for DroneRL.

These scripts exercise the SDK from outside the GUI loop and produce
reproducible artefacts in ``results/analysis/``. Each script is invokable as
``uv run python -m analysis.<name>``. Shared training helpers live in
``analysis._runner``.
"""
